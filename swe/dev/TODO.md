 - fix waterheight problem
 - remove computeNormals <--- seems to work
 - remove canvas from window (create a new one) <-- works
 - pass fb-texture to material-displacementMap instead of canvas <-- doesn't work


